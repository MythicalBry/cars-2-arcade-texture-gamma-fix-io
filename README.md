A script to patch .dds files at 0x74 from 00 to 01, making them appear gamma correct in Cars 2 Arcade.

Do not use this on textures that are meant to be used as normal maps!!! (aka end with `_n`)

Usage: `<script_file> -i <folder_path>`
